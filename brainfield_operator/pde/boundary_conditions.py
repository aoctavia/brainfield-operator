def build_dirichlet_bc_from_electrodes(grid_shape, electrode_masks, potentials):
    """
    Returns:
      bc_mask: bool array where potential is fixed
      bc_values: array with potential values at fixed nodes
    """
    ...
